pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'FoodLoop code is being processed by Jenkins'
            }
        }

        stage('Build') {
            steps {
                echo 'FoodLoop build successful'
            }
        }

    }
}
